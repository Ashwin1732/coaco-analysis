# Install torch/torchvision from the CPU index on machines without CUDA:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
#   pip install Pillow scikit-learn numpy tqdm
torch
torchvision
Pillow
scikit-learn
numpy
tqdm
